-- Миграция 013: короткий статус пользователя.
-- Показывается на профиле прямо под аватаром — в отличие от «О себе» (bio),
-- который остаётся длинным текстом внизу блока информации.
ALTER TABLE users
    ADD COLUMN IF NOT EXISTS status VARCHAR(120);
