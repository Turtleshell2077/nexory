-- Миграция 014: ссылка на покупку билета.
-- Заполняется только для платных мероприятий: организатор указывает, где купить билет.
-- В будущем планируется покупка прямо в приложении (см. ROADMAP.md).
ALTER TABLE events
    ADD COLUMN IF NOT EXISTS ticket_url VARCHAR(500);
